# JSnap

One-click page-to-JSON. BYOK. Free forever.

A polished README is generated in Stage 7. During build, this placeholder documents that the project is in active scaffolding. See `PLAN.md` for the architecture and `PROGRESS.md` for the live stage tracker.
